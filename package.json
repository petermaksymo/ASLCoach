{
  "name": "asl-coach",
  "version": "0.0.1",
  "private": false,
  "scripts": {
    "start": "docker-compose -f docker-compose.yml up -d --build",
    "lint": "docker exec -t planit_app npm run lint",
    "prettier": "docker exec -t planit_app npm run prettier",
  }
}
